import { StyleSheet } from '../Classes/StyleSheet.js';

const body = new StyleSheet();

body.rule('body')
    .set({fontFamily: 'Arial, sans-serif', padding: '0', margin: '0 auto', fontSize: '16px'})
export default body;
